[project]
name = "acars-bridge"
version = "0.2.3"
description = "Print-only Hoppie ACARS bridge for any aircraft client."
readme = "README.md"
authors = [
    { name = "Tebin Ulrich", email = "dlhairbus320@googlemail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "cryptography>=50.0.0",
    "httpx>=0.28.1",
    "pillow>=10.0.0",
    "platformdirs>=4.11.0",
    "pyside6>=6.8",
    "python-escpos>=3.1",
    "pydivert>=3.1.3; sys_platform == 'win32'",
    "pywin32>=311; sys_platform == 'win32'",
    "typer>=0.27.0",
]

[project.scripts]
acars-bridge = "acars_bridge.cli:app"

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pyinstaller>=6.14.0",
    "pytest>=9.1.1",
    "ruff>=0.16.1",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

